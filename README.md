# monitor_precos

Monitor automatizado de preços usando Python, BeautifulSoup e Bot do Telegram.

## Como configurar
1. Instale as dependências: `pip install -r requirements.txt`
2. Configure suas credenciais no arquivo `.env`
3. Adicione os links no `config.json`
4. Execute com `python main.py`
